https://www.cnblogs.com/tugenhua0707/p/4050072.html
https://blog.csdn.net/weixin_42701636/article/details/102905447?spm=1001.2014.3001.5501


Git --> Git Bash

配置：
git config  --global 参数
git config --global user.name "darren"
git config --global user.email "darren.deng@anker.com"

创建工作库：
新建文件夹
git init

提交文件：
git add readme.txt
git commit -m “提交readme文件”

查看状态：
git status

修改文件：
git diff readme.txt
git add readme.txt
git commit -m “修改readme文件”

查看提交日志：
git log
git log --pretty=oneline

版本回退：
git reset  --hard HEAD~100 //(前100个版本)
git reset  --hard 版本号   //回退到指定版本号

撤销修改：
git checkout --readme.txt

删除文件：
直接删除rm
git add a.txt
git commit -m "删除文件"


远程仓库提交代码步骤
https://blog.csdn.net/qwe122343/article/details/103837919

首先要在github创建仓库mysave

1. 打开git-bash.exe
2. 基础步骤
在git敲入两行命令 $后面的语句
$ git config --global user.name “你自己的github名字”
$ git config --global user.email “你自己的github注册邮箱”

3.（密钥生成 3 4这两步新手感觉比较繁琐 但必须要完成）
$ ssh-keygen -t rsa -C “你自己的github注册邮箱”
回车之后一路回车，等git加载完成，然后打开c盘目录找到id_rsa.pub，记事本打开
4. 进入github 增加生成的秘钥
5. 回到Git，在git敲入命令
6、/e/学习/git
7、git init
8、 git add .  //提交所有文件
9、git commit -m “git测试提交”
10、git remote add origin git@github.com:dazhanpengtu/mysave.git
11、git pull --rebase origin master  //拉代码，如果是第一次提交不需要此步骤
12、git push -u origin master   //提交代码




第一步、创建SSH Key
在用户主目录下，看看有没有.ssh目录，如果有，再看看这个目录下有没有id_rsa和id_rsa.pub这两个文件，
如果有的话，直接跳过此如下命令，如果没有的话，打开命令行，输入如下命令：
ssh-keygen  -t rsa –C “youremail@example.com”
id_rsa是私钥，不能泄露出去，id_rsa.pub是公钥，可以放心地告诉任何人。

第二步、登录github,打开” settings”中的SSH Keys页面，
然后点击“Add SSH Key”,填上任意title，在Key文本框里黏贴id_rsa.pub文件的内容。
点击 Add Key，你就应该可以看到已经添加的key。

添加远程库：
git remote add origin https://github.com/tugenhua0707/testgit.git

提交：
git push origin master



